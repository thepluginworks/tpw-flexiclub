# Identity Architecture

Identity architecture documentation for the shared plugin framework and the wider TPW platform lives in [identity-model.md](identity-model.md).

Start with [identity-model.md](identity-model.md) for the canonical TPW identity architecture specification.